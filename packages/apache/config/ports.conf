# HostPanel v3 — Apache Ports Configuration
# Apache runs strictly as an internal backend application server behind Nginx reverse proxy.
# Bound exclusively to loopback to prevent direct external network access.

Listen 127.0.0.1:8088
Listen [::1]:8088

