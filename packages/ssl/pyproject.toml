[project]
name = "hostpanel-ssld"
version = "3.0.0"
description = "HostPanel v3 SSL & ACME Certificate Package Daemon"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
    "pydantic>=2.6.0",
    "httpx>=0.27.0",
    "cryptography>=42.0.0"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
